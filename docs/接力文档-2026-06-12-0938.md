# MetaCode 项目接力文档

生成时间：2026-06-12 09:38:00 +08:00  
用途：新开 Codex 窗口后快速恢复 MetaCode 项目上下文。  
建议新窗口第一句话：`请先读取 D:\Virgil\Virgil\元代码项目Software DNA\源码\docs\接力文档-2026-06-12-0938.md，然后扫描项目代码，按文档继续下一阶段开发，不要改动锁定文件。`

## Project Snapshot

- 项目名：MetaCode / Software DNA 原型项目
- 项目根目录：`D:\Virgil\Virgil\元代码项目Software DNA\源码`
- 当前 Codex 工作目录曾是：`C:\Users\Administrator\Documents\Metacode`
- Git 分支：`main...origin/main`
- 最新提交：`00de301 Add Stage 13 repair event chains`
- 当前阶段：Stage 13 已完成
- 当前本地服务：未发现 `8765` 或 `8770` 端口有持续监听服务
- Dashboard 启动命令：`python serve_observatory.py --port 8770`
- Dashboard 地址：`http://127.0.0.1:8770/dashboard/`
- API 状态地址：`http://127.0.0.1:8770/api/status`

## Current Goal

下一阶段建议进入 Stage 14：一键修复 API 与修复详情页。

核心目标不是重写已有系统，而是在 Stage 13 的修复事件链基础上，补上可触发、可追踪、可复盘的修复入口，让 Dashboard 从“只读观察台”逐步接近“可控修复工作台”。

## Current State

项目已经完成从能力复用到可视化观察台的最小闭环：

- 能力单元：`metacodes/*/identity.yaml` + `runner.py`
- Workflow 执行：`run_workflow.py`
- 运行日志：`logs/run_log.jsonl`
- 失败日志：`logs/failure_log.jsonl`
- 缺失字段诊断：`core/recommender.py`
- 单步修复：`core/workflow_fixer.py`
- 多步路径规划：`core/path_planner.py`
- 能力图谱：`core/capability_graph.py`
- 监控导出：`export_monitoring_data.py`
- 本地 API：`serve_observatory.py` + `core/monitoring_api.py`
- Web 观察台：`dashboard/index.html`、`dashboard/styles.css`、`dashboard/app.js`
- 修复闭环统计：`monitoring/exports/repair_metrics.json`
- 修复事件链：`monitoring/exports/repair_events.json`

最新监控指标，来自 2026-06-12 09:37 左右本地测试刷新后的导出：

- `current_stage = 13`
- `metacode_count = 15`
- `stable_workflow_count = 10`
- `workflow_file_count = 13`
- `run_count = 625`
- `success_run_count = 462`
- `failure_run_count = 163`
- `success_rate = 73.9%`
- `edge_count = 26`
- `field_count = 16`
- `unresolved_field_count = 0`
- `repair_attempt_count = 138`
- `repair_success_count = 138`
- `repair_success_rate = 100.0%`
- `repair_event_count = 138`
- `repair_event_link_rate = 100.0%`

## Latest Verification

本接力文档生成前已重新执行验证：

```powershell
python -m unittest discover -s tests -p "*unittest.py" -v
```

结果：

```text
Ran 65 tests in 11.021s
OK
```

同时执行：

```powershell
node --check dashboard\app.js
```

结果：通过，无语法错误输出。

注意：这次全量测试会刷新运行日志和监控导出文件，所以测试后 Git 工作区出现了生成数据改动，不是代码结构改动。

当前测试产生的改动文件：

- `logs/failure_log.jsonl`
- `logs/run_log.jsonl`
- `monitoring/exports/dashboard_summary.json`
- `monitoring/exports/failures.json`
- `monitoring/exports/repair_events.json`
- `monitoring/exports/repair_metrics.json`
- `monitoring/exports/runs.json`

## Key Decisions

- 项目核心思想：不要让 AI 每次从零写代码，而是沉淀可复用能力单元、能力图谱、workflow 路径、失败诊断、自动修复和验证记录。
- Dashboard 的定位：工程观察台优先，不是营销页，也不是一开始就做复杂控制台。
- 每个阶段必须有新的验证报告，放在 `docs/Stage N 验证报告.md`。
- 项目进度要持续维护在 `docs/项目进度记录.md`。
- 修复相关能力必须形成闭环：failure -> suggestion -> generated workflow -> verification -> repair event。
- 扩展新能力时优先保持现有目录结构和命名风格，不随意重构。
- `PROJECT_STRUCTURE_LOCK.md` 是结构化锁定文件，只读；没有用户明确授权不得修改。

## Important Files

- `D:\Virgil\Virgil\元代码项目Software DNA\源码\PROJECT_STRUCTURE_LOCK.md`：项目结构锁定文件，只读。
- `D:\Virgil\Virgil\元代码项目Software DNA\源码\README.md`：项目中英文总说明，当前写到 Stage 13。
- `D:\Virgil\Virgil\元代码项目Software DNA\源码\docs\项目进度记录.md`：阶段推进记录，下一阶段完成后要更新。
- `D:\Virgil\Virgil\元代码项目Software DNA\源码\docs\Stage 13 验证报告.md`：最新阶段报告。
- `D:\Virgil\Virgil\元代码项目Software DNA\源码\core\monitoring_api.py`：本地 API 主要逻辑，Stage 14 大概率要改这里。
- `D:\Virgil\Virgil\元代码项目Software DNA\源码\serve_observatory.py`：本地 HTTP 服务入口。
- `D:\Virgil\Virgil\元代码项目Software DNA\源码\dashboard\app.js`：Dashboard 前端逻辑，Stage 14 需要增加修复入口和详情交互。
- `D:\Virgil\Virgil\元代码项目Software DNA\源码\dashboard\index.html`：Dashboard 页面结构。
- `D:\Virgil\Virgil\元代码项目Software DNA\源码\dashboard\styles.css`：Dashboard 视觉样式。
- `D:\Virgil\Virgil\元代码项目Software DNA\源码\core\workflow_fixer.py`：单步修复逻辑。
- `D:\Virgil\Virgil\元代码项目Software DNA\源码\core\path_planner.py`：多步规划逻辑。
- `D:\Virgil\Virgil\元代码项目Software DNA\源码\export_monitoring_data.py`：监控数据导出。
- `D:\Virgil\Virgil\元代码项目Software DNA\源码\tests\test_stage13_unittest.py`：Stage 13 测试，可作为 Stage 14 测试风格参考。

## How To Resume

新窗口建议按这个顺序做：

1. 读取本文件。
2. 进入项目根目录：`D:\Virgil\Virgil\元代码项目Software DNA\源码`
3. 执行：`git status --short --branch`
4. 读取：`PROJECT_STRUCTURE_LOCK.md`，但不要修改。
5. 读取：`README.md`、`docs\项目进度记录.md`、`docs\Stage 13 验证报告.md`
6. 扫描 Stage 14 相关代码：`core\monitoring_api.py`、`serve_observatory.py`、`dashboard\app.js`、`core\workflow_fixer.py`、`core\path_planner.py`
7. 先设计 Stage 14 最小闭环，再动代码。
8. 完成后必须新增 `docs\Stage 14 验证报告.md`，并更新项目进度记录。
9. 最后运行全量测试和 Dashboard 语法检查。

常用命令：

```powershell
pip install -r requirements.txt
python export_monitoring_data.py
python -m unittest discover -s tests -p "*unittest.py" -v
node --check dashboard\app.js
python serve_observatory.py --port 8770
```

## Next Steps

Stage 14 推荐范围：

1. 新增 `POST /api/repair/fix-workflow`。
2. 新增 `POST /api/repair/plan-workflow`。
3. 为 UI 触发的修复显式生成并写入 `repair_id`。
4. 将 repair event 从“历史 runs 推断”推进到“修复动作生命周期写入”。
5. Dashboard 增加修复详情抽屉或详情面板。
6. 详情中展示完整链路：failure、suggestion、generated workflow、verification。
7. 补 `tests/test_stage14_unittest.py`。
8. 新增 `docs/Stage 14 验证报告.md`。
9. 更新 `docs/项目进度记录.md` 和 `README.md`。

Stage 14 要保持最小闭环，不建议一口气做复杂权限、多用户、远程部署、插件系统。

## Risks And Watchpoints

- 现在的 repair event 仍主要来自历史 runs 推断；Stage 14 要避免把推断逻辑和真实触发逻辑混在一起。
- Dashboard 目前已经有不少模块，新增修复入口时要控制界面密度，避免页面变成堆叠信息墙。
- API 目前是本地原型，不要提前引入复杂框架迁移。
- 测试会刷新 logs 和 monitoring exports，新窗口不要误判为用户手写代码改动。
- 终端读取部分中文 Markdown 可能显示乱码，优先参考英文 README、JSON 数据、代码结构和本接力文档。
- 生成的 workflow 文件要继续放在 `workflows/generated/`，不要直接覆盖稳定 workflow。

## Do Not Change Without Permission

- 不要修改 `D:\Virgil\Virgil\元代码项目Software DNA\源码\PROJECT_STRUCTURE_LOCK.md`。
- 不要大规模重构目录结构。
- 不要删除历史阶段报告。
- 不要清空或重写 `logs/` 历史记录。
- 不要把 generated workflow 直接提升为 stable workflow，除非用户明确要求。
- 不要为了界面美化牺牲观察台的信息密度和工程可读性。

## Git Notes For New Window

生成本接力文档前，仓库为 `main...origin/main`。执行全量测试后，日志和监控导出出现改动；本文件本身也是新增文档。新窗口应先查看 `git status --short --branch`，判断是否保留这些验证数据改动。

如果用户要提交，建议提交前再次确认：

```powershell
git status --short --branch
python -m unittest discover -s tests -p "*unittest.py" -v
node --check dashboard\app.js
```
